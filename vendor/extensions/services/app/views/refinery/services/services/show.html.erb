<% content_for :body_content_title do %>
  <%= @service.name %>
<% end %>

<% content_for :body do %>
  <section>
    <h1>Name</h1>
    <p>
      <%=raw @service.name %>
    </p>
  </section>
  <section>
    <h1>Description</h1>
    <p>
      <%=raw @service.description %>
    </p>
  </section>
  <section>
    <h1>Photo</h1>
    <p>
      <%= image_fu @service.photo, nil %>
    </p>
  </section>
<% end %>

<% content_for :side_body do %>
  <aside>
    <h2><%= t('.other') %></h2>
    <ul id="services">
      <% @services.each do |service| %>
        <li>
          <%= link_to service.name, refinery.services_service_path(service) %>
        </li>
      <% end %>
    </ul>
  </aside>
<% end %>

<%= render '/refinery/content_page' %>
